<center>
<div class="">
<div> <%= link_to "Pay on delivery" , payment_success_path(@cart_products,params[:quantity]) %> </div>

<div>
	<br>
<%= form_tag charges_path do %>
  <article>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>
   <!--  <label class="amount">
      <span>Amount: $5.00</span>
    </label> -->
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button" data-key="<%= Rails.configuration.stripe[:publishable_key] %>" data-description="A month's subscription"   data-email="<%= current_user.email %>" data-amount="<%=params[:total]%>" data-locale="auto"></script>
<% end %>

</div>
</div>
</center>